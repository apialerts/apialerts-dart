name: apialerts
description: Dart client for the API Alerts notification platform. Send once, deliver everywhere.
version: 1.0.0
homepage: https://apialerts.com
repository: https://github.com/apialerts/apialerts-dart
issue_tracker: https://github.com/apialerts/apialerts-dart/issues
topics:
  - notifications
  - alerts
  - monitoring
  - sdk

environment:
  sdk: ^3.0.0

dependencies:
  http: ^1.6.0

dev_dependencies:
  test: ^1.25.0
  lints: ^6.1.0
  shelf: ^1.4.0
